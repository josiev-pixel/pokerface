# Claude project memory

Claude auto-loads `CLAUDE.md` (not `AGENTS.md`), so this imports the project's
agent notes below. The workspace orchestrator role, delegation policy, and
engineering standards load automatically from `..\CLAUDE.md`.

@AGENTS.md
